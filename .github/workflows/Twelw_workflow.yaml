name: Twelwe Workflow
on: [push]
  
jobs:
    run_first-command:
        runs-on: ubuntu-latest
        environment: "prod"
        steps:
          - name: Varsa and Secret
            run: |
              echo '${{ vars.STATUS }}'
              echo '${{ secrets.PASSWORD }}'
              